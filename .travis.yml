language: node_js
node_js:
  - 12

os:
  - linux

cache:
  bundler: true
  directories:
    - node_modules

install:
  - make install

stages:
  - test
  - name: deploy
    if: (type = push) AND branch =~ ^(master|v\d+\.\d+)$

jobs:
  include:

    - stage: test
      script:
        - make test
        - make build

    - stage: deploy
      deploy:
        provider: surge
        project: ./docs/_site/
        domain: ai-ml.club
        skip_cleanup: true

notifications:
  email:
    on_success: change
    on_failure: change
